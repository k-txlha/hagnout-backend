version: '3.8'

services:
  backend:
    build: 
      context : .
      dockerfile: Dockerfile

    image: hangout-backend
    
    ports:
      - "${PORT}:${PORT}"
  
    volumes:
      - ./:/app
      - /app/node_modules
    
    env_file:
      .env
